import React from 'react';
import Container from './Container';

const Loading = () => {
    return (
        <div>
            <Container>
                <h1>LOADING</h1>
            </Container>
        </div>
    );
};

export default Loading;